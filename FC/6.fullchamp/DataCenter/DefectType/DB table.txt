create table SYS_DEFECT_TYPE_EMP
(
  TYPE_EMP_ID    NUMBER PRIMARY KEY,
  EMP_ID         NUMBER,
  DEFECT_TYPE_ID NUMBER,
  ENABLED        VARCHAR2(20) default 'Y',
  UPDATE_USER_ID NUMBER,
  UPDATE_TIME    DATE
)