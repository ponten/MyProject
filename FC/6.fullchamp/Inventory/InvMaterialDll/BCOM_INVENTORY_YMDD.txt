CREATE OR REPLACE FUNCTION 
sajet.BCOM_INVENTORY_YMDD return Varchar2 is

SRETURN VARCHAR2(10);
BEGIN
   SRETURN:='';
   RETURN SRETURN;

EXCEPTION
  WHEN OTHERS THEN
     RETURN SQLERRM;
END;
/

grant execute on BCOM_INVENTORY_YMDD to sys_user;
grant execute on BCOM_INVENTORY_YMDD to sj;
